---
description: Show traffic / view counts for the user's clawpage pages.
---

Run the `view-stats` skill to fetch page-view counts and per-day series for the user's pages, user homepage, and short links. Use `--overview` for the cross-page summary, `--page <name>` / `--home` / `--link <slug>` for a specific surface.
